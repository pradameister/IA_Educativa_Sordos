{
  "name": "ia-educativa-sordos",
  "version": "1.0.0",
  "description": "Aplicación web con profesor virtual de IA para enseñanza de programación orientada a objetos, con accesibilidad para personas sordas",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w frontend\" \"npm run dev -w backend\"",
    "build": "npm run build -w shared && npm run build -w backend && npm run build -w frontend",
    "lint": "npm run lint -w frontend && npm run lint -w backend"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
